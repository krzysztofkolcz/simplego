apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-go-rest-foundation-chart.fullname" . }}
  labels:
    {{- include "my-go-rest-foundation-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-go-rest-foundation-chart.selectorLabels" . | nindent 4 }}
