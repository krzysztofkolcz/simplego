# removing this section, and moving passwords to ./go-hello/templates/sealedsecret-postgres.yaml

# Only for Bitnami!!! Error pulling image
# image:
#   tag: "17"

# Only for Bitnami!!!
auth:
  username: appuser
  password: strongpassword
  database: appdb
  postgresPassword: superadminpassword

primary:
  persistence:
    enabled: true
    size: 10Gi

  resources:
    requests:
      memory: 256Mi
      cpu: 250m
    limits:
      memory: 512Mi
      cpu: 500m
