# removing this section, and moving passwords to ./go-hello/templates/sealedsecret-postgres.yaml
# auth:
#   username: 
#   password: 
#   database: 
#   postgresPassword: 

primary:
  persistence:
    enabled: true
    size: 10Gi

  resources:
    requests:
      memory: 256Mi
      cpu: 250m
    limits:
      memory: 512Mi
      cpu: 500m
